# Dependencies
node_modules/

# Environment Variables (CRITICAL: Do not upload passwords)
.env
.env.*
!.env.example

# Next.js / Vite Build output
.next/
dist/
build/
.output/

# API File Uploads (Local Storage) — user PII, never commit.
# Anchored on purpose: a bare `uploads/` also swallows source
# directories such as apps/api/src/modules/uploads/.
apps/api/uploads/

# OS Files
.DS_Store
Thumbs.db

#Comment by Pranay
