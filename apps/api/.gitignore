node_modules
dist

# Keep environment variables out of version control.
# Every .env variant is ignored; only the placeholder example is committed.
.env*
!.env.example

# Uploaded documents are user PII — never commit them
/uploads

/src/generated/prisma
